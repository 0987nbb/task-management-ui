import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aliraza.taskbasic',
  appName: 'Task Basic',
  webDir: 'dist/task-basic/browser'
};

export default config;
